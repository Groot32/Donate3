const donate3Abi = require("./donate3.abi.json")
const donateEntryAbi = require("./donateEntry.abi.json")
const contractAddress = require("./contractAddress.json")
const tableName = require("./tableName.json")
const donate3ContractAddress = contractAddress.donate3
module.exports = { donate3Abi, donateEntryAbi, donate3ContractAddress, tableName }
